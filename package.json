{
  "name": "real-estate-layout-tool",
  "version": "1.0.0",
  "description": "Layout tool experiment for real estate listings",
  "dependencies": {},
  "devDependencies": {
    "parcel": "~1.12.4",
    "pug": "~3.0.0"
  },
  "scripts": {
    "start": "parcel index.pug --port 5000",
    "build": "rm -r dist && NODE_ENV=production parcel build index.pug",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Todd Wolfson <todd@twolfson.com> (http://twolfson.com/)",
  "license": "Unlicense",
  "private": true
}
